
var materilizedFractorEmancer = require('./bs/refract')
//Re materialize object

exports.pop =function(){
  var piton = materilizedFractorEmancer.calculateEmulsion("NEW_DIVEGENT_EVENT",10,Date.now())
  var verbArgs;
  var _prepend = Math.ceil(Math.round(piton.length)+ Math.random())
  var ext = piton.split('').forEach(function(i){
    var tracky = _prepend+(i + i +" --Serial--")
  })
  return piton;
}

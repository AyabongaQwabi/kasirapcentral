var preculiar ="emul.void"

exports.calculateEmulsion = function(spaceProtrusion,mass,dominantTime){
  if(spaceProtrusion){
    return preculiar;
  }
  else{
    spaceProtrusion = mass * dominantTime;
    return Math.ceil(spaceProtrusion)
  }

}
